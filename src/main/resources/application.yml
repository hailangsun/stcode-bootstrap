
server:
  tomcat:
    uri-encoding: UTF-8
  port: 8089


# ��־����
logging:
  root: info
  level:
    com:
      stcode: debug




spring:
  profiles:
    active: oracle


# ǰ��thymeleafģ������
    thymeleaf:
      prefix: classpath:/templates/
      suffix: .html
      enabled: true
      cache: false
      encoding: UTF-8
      mode: HTML5



mybatis:
  mapper-locations: classpath:mapper/**/*.xml
#  mapper-locations: com/stcode/bootstrap/mapper/**/*.xml
